- Initialize a new project
- get your entry point
- update your entry point with the shebang line of code to make your file executatble by node with a command
- update package.json with the command
- make you package avilable locally for testing
- test your script with a console log

- tell node where to store your data
- initialize a storage system for your data
  - check if storage dir exist else create one
  - check if storage file exists else create one
  - we need to be able to read all files from storage
  - we also need to be able to write tasks to storage
